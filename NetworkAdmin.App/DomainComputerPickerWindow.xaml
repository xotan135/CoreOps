<Window x:Class="NetworkAdmin.App.DomainComputerPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select domain computers" Icon="/Assets/CoreOps.ico"
        Width="940" Height="620" MinWidth="680" MinHeight="430"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="CanResize"
        Background="#151515" Foreground="#F1F1F1" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#F1F1F1"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="9"/><Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="Background" Value="#181818"/><Setter Property="BorderBrush" Value="#484848"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/><Setter Property="SelectionBrush" Value="#476F6C"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/><Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F4F4F4"/><Setter Property="Background" Value="#353535"/>
            <Setter Property="BorderBrush" Value="#646464"/><Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#FFFFFF"/><Setter Property="Background" Value="#303030"/>
            <Setter Property="BorderBrush" Value="#606060"/><Setter Property="Padding" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Background="#303030" BorderBrush="#606060" BorderThickness="1">
                                <Grid Margin="9,0,7,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="18"/></Grid.ColumnDefinitions>
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      TextElement.Foreground="#FFFFFF"/>
                                    <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#D8D8D8"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True">
                                <Border Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="#2B2B2B" BorderBrush="#606060" BorderThickness="1" Padding="1">
                                    <ScrollViewer MaxHeight="300" CanContentScroll="True"><ItemsPresenter/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#FFFFFF"/><Setter Property="Background" Value="#2B2B2B"/>
            <Setter Property="Padding" Value="8,5"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True"><Setter Property="Background" Value="#505050"/></Trigger>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#315C59"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#303030"/><Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#484848"/><Setter Property="Padding" Value="9,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
            <Setter Property="Background" Value="Transparent"/><Setter Property="BorderBrush" Value="#303030"/>
            <Setter Property="Padding" Value="8,6"/><Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#1B1B1B"/><Setter Property="Foreground" Value="#E5E5E5"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1"><Setter Property="Background" Value="#222222"/></Trigger>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#2B2B2B"/></Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#315C59"/><Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/><RowDefinition Height="*"/><RowDefinition Height="12"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Select domain computers" FontFamily="Bahnschrift SemiCondensed" FontSize="25" FontWeight="SemiBold"/>
            <TextBlock Text="Search or filter the directory, click a column heading to sort, then select one or more computers."
                       Foreground="#AFAFAF" Margin="0,3,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="145"/><ColumnDefinition Width="260"/><ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox" TextChanged="FilterChanged" ToolTip="Enter filter text"/>
            <ComboBox x:Name="SearchFieldComboBox" Grid.Column="1" Margin="8,0,0,0" SelectedIndex="0"
                      SelectionChanged="FilterChanged" ToolTip="Choose which field to search">
                <ComboBoxItem Content="All fields"/><ComboBoxItem Content="Computer name"/>
                <ComboBoxItem Content="Operating system"/><ComboBoxItem Content="OS version"/>
            </ComboBox>
            <ComboBox x:Name="OperatingSystemFilterComboBox" Grid.Column="2" Margin="8,0,0,0" SelectedIndex="0"
                      SelectionChanged="FilterChanged" ToolTip="Show one operating-system version"/>
            <Button x:Name="RefreshButton" Grid.Column="3" Content="Refresh" Click="RefreshButton_Click"/>
        </Grid>

        <DataGrid x:Name="ComputerGrid" Grid.Row="4" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserSortColumns="True" SelectionMode="Extended" SelectionUnit="FullRow"
                  Background="#181818" Foreground="#E5E5E5" BorderBrush="#444444" AlternationCount="2"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#303030" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Computer" Binding="{Binding TargetName}" SortMemberPath="TargetName" Width="2*"/>
                <DataGridTextColumn Header="Operating system" Binding="{Binding OperatingSystem}" SortMemberPath="OperatingSystem" Width="3*"/>
                <DataGridTextColumn Header="OS version" Binding="{Binding OperatingSystemVersion}" SortMemberPath="OperatingSystemVersion" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusTextBlock" Foreground="#AFAFAF" VerticalAlignment="Center"
                       Text="Loading domain computers…" TextTrimming="CharacterEllipsis" Margin="0,0,12,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Select all shown" Click="SelectAllButton_Click"/>
                <Button Content="Cancel" IsCancel="True"/>
                <Button x:Name="AddButton" Content="Add selected" IsDefault="True" Click="AddButton_Click"
                        Background="#315C59" BorderBrush="#72DAD4"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
