<Window x:Class="NetworkAdmin.App.DomainComputerPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select domain computers" Icon="/Assets/CoreOps.ico"
        Width="820" Height="620" MinWidth="620" MinHeight="440"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="#151515" Foreground="#F1F1F1" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#F1F1F1"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="9"/><Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="Background" Value="#181818"/><Setter Property="BorderBrush" Value="#484848"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/><Setter Property="SelectionBrush" Value="#476F6C"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/><Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F4F4F4"/><Setter Property="Background" Value="#353535"/>
            <Setter Property="BorderBrush" Value="#646464"/><Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Background="#1B1B1B" BorderBrush="#303030"
                                BorderThickness="0,0,0,1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#292929"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#315C59"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#72DAD4"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="ItemBorder" Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/><RowDefinition Height="*"/><RowDefinition Height="12"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Select domain computers" FontFamily="Bahnschrift SemiCondensed" FontSize="25" FontWeight="SemiBold"/>
            <TextBlock Text="Choose one or more enabled computer accounts from Active Directory."
                       Foreground="#AFAFAF" Margin="0,3,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox" TextChanged="SearchTextBox_TextChanged"
                     ToolTip="Filter by computer name or operating system"/>
            <Button x:Name="RefreshButton" Grid.Column="1" Content="Refresh" Click="RefreshButton_Click"/>
        </Grid>

        <Border Grid.Row="4" BorderBrush="#444444" BorderThickness="1" Background="#181818">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Grid Background="#2D2D2D">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="3*"/></Grid.ColumnDefinitions>
                    <TextBlock Text="Computer" FontWeight="SemiBold" Padding="9,7"/>
                    <TextBlock Grid.Column="1" Text="Operating system" FontWeight="SemiBold" Padding="9,7"/>
                </Grid>
                <ListBox x:Name="ComputerListBox" Grid.Row="1" SelectionMode="Extended"
                         Background="#181818" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="3*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding TargetName}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="1" Text="{Binding OperatingSystem}" Margin="12,0,0,0"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusTextBlock" Foreground="#AFAFAF" VerticalAlignment="Center"
                       Text="Loading domain computers…" TextTrimming="CharacterEllipsis" Margin="0,0,12,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Select all shown" Click="SelectAllButton_Click"/>
                <Button Content="Cancel" IsCancel="True"/>
                <Button x:Name="AddButton" Content="Add selected" IsDefault="True" Click="AddButton_Click"
                        Background="#315C59" BorderBrush="#72DAD4"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
